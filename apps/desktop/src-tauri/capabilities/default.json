{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the Flight Finder launcher window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
